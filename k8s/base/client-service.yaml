apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: todo-app
spec:
  selector:
    app: client
  ports:
  - port: 5173
    targetPort: 5173
    protocol: TCP
    name: http
  type: ClusterIP
