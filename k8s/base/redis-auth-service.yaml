apiVersion: v1
kind: Service
metadata:
  name: redis-auth
  namespace: todo-app
spec:
  selector:
    app: redis-auth
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  type: ClusterIP
