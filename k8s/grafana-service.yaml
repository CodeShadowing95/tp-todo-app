apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: todo-app
spec:
  selector:
    app: grafana
  ports:
  - port: 3001
    targetPort: 3000
    protocol: TCP
    name: http
  type: LoadBalancer
